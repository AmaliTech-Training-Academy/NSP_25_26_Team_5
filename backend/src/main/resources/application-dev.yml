server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/communityboard
    username: postgres
    password: Annet@25437

  mail:
    host: localhost
    port: 1025
    username: dev@example.com
    password: dev-pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
