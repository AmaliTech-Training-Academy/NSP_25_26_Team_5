server:
  port: 8081

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/communityboard}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Annet@25437}

  mail:
    host: localhost
    port: 1025
    username: dev@example.com
    password: dev-pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    user:
      name: none
      password: none   # <-- correct way to disable default user

profiles:
  active: dev   # dev is default

jwt:
  secret: ${JWT_SECRET:communityboard-secret-key-amalitech-2024}
  expiration: 86400000

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
