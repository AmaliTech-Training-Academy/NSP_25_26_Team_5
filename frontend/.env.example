# API base URL (include /api so paths like /auth/login become .../api/auth/login).
# Used at build time: set in Docker via --build-arg, or here for local dev.
VITE_API_BASE_URL=http://localhost:8080/api
